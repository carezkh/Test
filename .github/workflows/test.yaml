name: build

on:
  - push
  - pull_request

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - name: setup sanlock
        run: apt-get update && apt-get install sanlock/focal-backports
      
      - name: sanlock help
        run: sanlock --help
